---
name: Continuous Integration
on:
  push:
    branches:
      - main

#  workflow_dispatch:
jobs:
  build:
    name: print
    runs-on: ubuntu-latest
    steps:
      - name: print workflow name
        run: echo "first workflow"
